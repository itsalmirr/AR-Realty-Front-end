import { Layout } from '@components/layouts'
import { Home } from '@modules/home/index'

const HomePage = () => {
  return (
    <Layout title={'Home'}>
      <Home />
    </Layout>
  )
}

export default HomePage
