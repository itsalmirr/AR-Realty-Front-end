import { Layout } from '@components/index'

const Settings = () => {
  return (
    <Layout title={'Settings'}>
      <div>
        <h1>Settings</h1>
      </div>
    </Layout>
  )
}

export default Settings
